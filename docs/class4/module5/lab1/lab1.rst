DNS is B0rk3n
=====================================================

www.example.com is resolving from stale DNS servers.

Internal DNS servers are authoritative for example.com.

DNS traffic is flowing through an F5 device so we can override the stale example.com DNS zone

.. toctree::
   :hidden:
   :glob:

